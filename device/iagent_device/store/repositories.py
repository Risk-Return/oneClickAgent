"""SQLite repositories: device_info, agents (pool state: idle/busy with user_id + job_id),
jobs (in-flight + outbox), files, device_skills, agent_skills.
Queries for local execution detail, pool management, and tunnel outbox durability.
"""
