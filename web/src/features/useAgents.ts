// TanStack Query hooks: useAgents, useAgent (for customer's currently-allocated agents).
// useAdminAgents, useDrainAgent, useReleaseAgent (admin pool management).
// No useCreateAgent — agents are auto-allocated per job.
