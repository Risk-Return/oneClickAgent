// Customer "active agents" view: shows agents currently allocated to the
// caller's active jobs. Displays which job each is running, status (BUSY),
// live resource usage bars, agent tags. No creation — agents are pooled.
// (Admin-only pool management is in admin/DeviceFleetPage.)
