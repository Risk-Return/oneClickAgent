// Central job workspace: command input (textarea/structured), file upload drag-and-drop,
// single-select skill picker (at most one), submit button, live progress view
// with status pill (includes QUEUED state with position badge + estimated wait),
// progress bar, streaming messages, cancel button (enabled while queued or running),
// result display. Handles QUEUE_FULL error and QUEUE_TIMEOUT terminal state.
