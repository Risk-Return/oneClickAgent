// Admin device fleet: list all devices + online status, resources, hosted agent pool.
// Add device (enrollment code), rename, rotate token, decommission, set pool size.
// Also: manage user tiers (free/pro/enterprise) for queue priority.
