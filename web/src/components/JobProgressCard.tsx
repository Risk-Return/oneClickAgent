// Job progress display: status pill (includes "In Queue" state with position
// badge and estimated wait), progress bar (0-100%), streaming human-readable
// messages, elapsed time, cancel button (enabled in queue and running states).
