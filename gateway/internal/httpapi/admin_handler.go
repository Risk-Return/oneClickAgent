// Admin-only handlers: organization CRUD + membership, user tier management
// (PATCH /admin/users/{id}/tier), plus any cross-cutting admin operations
// not covered by other handler files.
package httpapi
