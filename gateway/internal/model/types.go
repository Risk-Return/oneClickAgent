// Package model defines domain types, DTOs, enums (job states including QUEUED,
// device states, agent pool states: creating/idle/busy/unhealthy/failed/removed,
// skill states, user tier: free/pro/enterprise, roles,
// error codes: QUEUE_TIMEOUT, QUEUE_FULL),
// and shared value objects (UUIDv7, timestamps).
package model
