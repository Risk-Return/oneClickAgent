// Package model defines domain types, DTOs, enums (job states, device states,
// agent pool states: creating/idle/busy/unhealthy/failed/removed,
// skill states, roles), and shared value objects (UUIDv7, timestamps).
package model
